<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="container">
      <div class="container-fluid" >
          <!-- <a class="navbar-brand" href="#">Navbar</a> -->
          <!-- <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button> -->
          <div class="collapse navbar-collapse" id="navbarSupportedContent" >
            <a href="/" class="navbar-brand" style="text-decoration: none; color: #3a6cf4; text-transform: uppercase; font-weight:700; font: size 1.8em; ">Moustafa</a>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link <%= pageTitle == 'Home' && 'active' %>" aria-current="page" href="/">Home</a>
              </li>
              <% if(isUser) { %>
                <input id="userId" type="hidden" value="<%= isUser %>">
                <li class="nav-item">
                  <a class="nav-link <%= pageTitle == 'Groups' && 'active' %>" href="/groups">Groups</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link <%= pageTitle == 'Friends' && 'active' %>" href="/chat/friends">Friends</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link <%= pageTitle == pageTitle && 'active' %>" href="/profile">Profile</a>
                </li>
                <div class="dropdown">
                  <button class="btn btn-primary dropdown-toggle" type="button" id="friendRequestsDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Friend Requests
                  </button>
                  <div id="friendRequests" class="dropdown-menu" aria-labelledby="friendRequestsDropdown">
                    <% if (friendsRequests.length === 0) { %>
                      <span>No Friends Requests</span>
                    <% } %>
                    <% for (let request of friendsRequests) { %>

                      <a class="dropdown-item" href="/profile/<%= request.id %>">
                        <%= request.name %>
                      </a>
                    <% } %>
                  </div>
                </div>
              <% } else { %>
                <li class="nav-item">
                  <a class="nav-link <%= pageTitle == 'Login' && 'active' %>" href="/login">Login</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link <%= pageTitle == 'SignUp' && 'active' %>" href="/signup">SignUp</a>
                </li>
              <% } %>
              
            </ul>
            <% if(isUser) { %>
              <form action="/logout" method="POST">
                <input type="submit" value="Logout" class="btn btn-danger">
              </form>
            <% } %>
          </div>
        </div>
    </div>
    </nav>